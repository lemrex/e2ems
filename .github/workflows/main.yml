name: CI/CD to Huawei SWR and CCE

on:
  push:
    branches:
      - main

env:
  REGION: af-south-1
  REGISTRY: swr.af-south-1.myhuaweicloud.com/ralf

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3


      - name: Build and push Docker images
        run: |

          

          docker login -u af-south-1@HST3UDJLC3R8N80P8BMS -p c40201c354311ca6051d8b8a54345fd6d060a6049f17ce6d3327be30050c537e swr.af-south-1.myhuaweicloud.com
          
          docker build --build-arg NEXT_PUBLIC_AUTH_API=http://121.91.156.139:3000 --build-arg NEXT_PUBLIC_TRANSACTION_API=http://121.91.156.139:4000 -t $REGISTRY/frontend-f:latesta ./frontend
          docker push $REGISTRY/frontend-f:latesta

  
          # docker build -t $REGISTRY/authentication-f:${{ github.sha }} ./authentication
          # docker push $REGISTRY/authentication-f:${{ github.sha }}

          # docker build -t $REGISTRY/transaction-f:${{ github.sha }} ./transaction
          # docker push $REGISTRY/transaction-f:${{ github.sha }}



